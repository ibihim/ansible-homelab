---
# This file should be encrypted with ansible-vault.
# Run: ansible-vault encrypt vars/secrets.yml
#
# To edit: ansible-vault edit vars/secrets.yml
# To run playbook: ansible-playbook playbook.yml --ask-vault-pass
#
# Placeholder values -- replace with real secrets before encrypting.

# These are NOT used by the playbook directly. The actual service secrets
# are managed by sops + age (see the 'secrets' role). These vault entries
# are here only if you want to bootstrap the sops secrets.yaml from Ansible.

# immich_db_password: "CHANGE_ME"
# nextcloud_db_password: "CHANGE_ME"
# nextcloud_admin_user: "CHANGE_ME"
# nextcloud_admin_password: "CHANGE_ME"
